# Example generator: openssl rand -hex 32
# Or just run the command: `echo "SEARXNG_SECRET_KEY=$(openssl rand -hex 32)" >> .env`
SEARXNG_SECRET_KEY=change-me-to-a-long-random-token

SEARXNG_PORT=8080
SEARXNG_BIND_ADDRESS=0.0.0.0
